name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: notion
          POSTGRES_PASSWORD: notion_ci
          POSTGRES_DB: my_notion_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build
        working-directory: apps/server
        run: go build ./...

      - name: Vet
        working-directory: apps/server
        run: go vet ./...

      - name: Test
        working-directory: apps/server
        run: go test ./... -count=1
        env:
          DATABASE_URL: "postgres://notion:notion_ci@localhost:5432/my_notion_test?sslmode=disable"
          JWT_SECRET: "ci-test-secret"

  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: apps/web
        run: pnpm build

      - name: Test
        working-directory: apps/web
        run: pnpm test
